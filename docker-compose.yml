version: '3.8'
services:
  app:
    build:
      context: .
      dockerfile: dockerfile
    env_file:
      - DjangoWebSite/docker.env
    volumes:
      - .:/srv/app
    ports:
      - "0.0.0.0:8000:8000"
    command: python manage.py runserver 0.0.0.0:8000

volumes:
  static-data:

